spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:64117/database?createDatabaseIfNotExist=true&useSSL=true
    username: root
    password: root-password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

logging:
  level:
    org.springframework.security: debug
